# Sensitive config — NEVER commit these
data/config.json

# Excel / data files (may contain company data)
data/*.xlsx
data/*.json
data/scans/

# Node
node_modules/
npm-debug.log*

# OS
.DS_Store
Thumbs.db

# Uploads
uploads/

#images that shouldnt be here
*.jpg
*.jpeg
*.png
*.gif

# Ignore everything in data/ — it's local/generated content, not source
data/*
!data/.gitkeep